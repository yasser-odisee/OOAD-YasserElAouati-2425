<Window x:Class="WPFComplexiteit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPFComplexiteit" Height="200" Width="350">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="txtInput" Width="200" Margin="0,0,10,0" Grid.Column="0"/>

        <Button x:Name="btnAnalyseer" Content="ANALYSEER" Width="80" Grid.Column="1" Click="btnAnalyseer_Click"/>

        <TextBlock x:Name="txtResult" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,10,0,0"/>
    </Grid>
</Window>
